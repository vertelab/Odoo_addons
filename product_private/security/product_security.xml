<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data noupdate="0">

        <record id="rule_product_template" model="ir.rule">
            <field name="name">Product Template Groups Restriction</field>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="domain_force">[('website_published', '=', True), ("sale_ok", "=", True), ('product_variant_ids.active', '=', True),
     '|', '|',
         ('access_group_ids', '=', False),
         '!', ('access_group_ids', '=', user.env.ref("webshop_dermanord.group_dn_salon").id),
         ('access_group_ids', 'in', [g.id for g in user.partner_id.commercial_partner_id.access_group_ids.filtered(lambda group: group == user.env.ref("webshop_dermanord.group_dn_salon"))])]</field>
        </record>

        <record id="rule_product_product" model="ir.rule">
            <field name="name">Product Groups Restriction</field>
            <field name="model_id" ref="product.model_product_product"/>
            <field name="domain_force">[('website_published', '=', True), ("sale_ok", "=", True), '|', '|',
    ('access_group_ids', '=', False),
    '!', ('access_group_ids', '=', user.env.ref("webshop_dermanord.group_dn_salon").id),
    ('access_group_ids', 'in', [g.id for g in user.partner_id.commercial_partner_id.access_group_ids.filtered(lambda group: group == user.env.ref("webshop_dermanord.group_dn_salon"))]),
'|', '|',
    ('product_tmpl_id.access_group_ids', '=', False),
    '!', ('product_tmpl_id.access_group_ids', '=', user.env.ref("webshop_dermanord.group_dn_salon").id),
    ('product_tmpl_id.access_group_ids', 'in', [g.id for g in user.partner_id.commercial_partner_id.access_group_ids.filtered(lambda group: group == user.env.ref("webshop_dermanord.group_dn_salon"))])]</field>
        </record>

    </data>
</openerp>
